Entrega 3b — ARREGLO URGENTE del arranque roto en la Entrega 3
Caché SW: mq-entrega3b-arreglo
La app debe mostrar en VOS: 'Entrega 3b · arreglo · datos v5'

EL BUG:
En la Entrega 3, missions.js empezó a pedirle el contexto a engine.js,
pero app.js inicializaba initMisiones ANTES que initEngine. Al abrir la
app sin misión del día, missions.js explotaba y cortaba la secuencia de
arranque: motor, diario, economía, avatar, cuarto y logros nunca se
inicializaban.

EL ARREGLO (tres capas):
  1. js/app.js    — initEngine ahora va antes que initMisiones
  2. js/engine.js — sugerirMision() devuelve null si no tiene datos,
                    en vez de explotar
  3. js/missions.js — la pantalla funciona igual sin sugerencia

EL AGUJERO TAPADO:
  tests/suite-arranque.js (nuevo) — replica el arranque REAL de app.js,
  leyendo el orden de init directamente del archivo. Si alguien vuelve a
  reordenarlo, el test avisa.

Sin cambios de datos: sigue en v5.
Suite: 75 tests. Todo en verde.
