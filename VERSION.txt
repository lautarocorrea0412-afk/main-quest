Entrega 4 — Tu historia + recordatorio de backup (C-25, C-26 + E1)
Caché SW: mq-entrega4-historia
La app debe mostrar en VOS: 'Entrega 4 · historia · datos v6'

DATA_VERSION sube a 6: el perfil ahora recuerda la fecha del último
backup. Migración automática, no se pierde nada.

Nuevos:
  js/history.js  (une misiones + diario + timeline en una vista navegable)

Modificados:
  js/store.js  (v6 + exportar() registra la fecha del backup)
  js/util.js   (estadoBackup)
  js/app.js    (init de historia, aviso de backup, puntito en la pestaña VOS)
  index.html, css/app.css, sw.js
  tests/suite-store.js, tests/suite-arranque.js, tests/suite-historia.js (nuevo)

Suite: 82 tests. Todo en verde.

C-25 y E1 resueltos juntos: la timeline se venía llenando sola desde la
Fase 2 (compras y logros) y nunca se veía. Ahora aparece dentro de cada día.
