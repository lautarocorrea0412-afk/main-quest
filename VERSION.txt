Entrega 17b — Agenda arreglada
Caché SW: mq-entrega17b-agenda
La app debe mostrar en VOS: 'Entrega 17b · agenda arreglada · datos v13'

Sin cambios de datos.

DOS BUGS que impedían usar la agenda, arreglados:

1. LA AGENDA NO SE INICIALIZABA. En la 17, la llamada initAgenda(data)
   nunca se agregó al arranque (se me escapó en el cableado). El panel
   quedaba a medio armar y por eso "no dejaba escribir ni nada". Ahora
   sí se inicializa. De paso descubrí que initSonido(data) TAMPOCO estaba
   en el arranque —el sonido solo se activaba al entrar por la ceremonia—;
   también lo agregué.

2. EL BOTÓN "+ AGENDAR ALGO" NO ABRÍA LA HOJA. Usaba un import dinámico
   de ui.js —import("./ui.js")— que es frágil en la PWA con service
   worker de iOS. Lo cambié a import estático arriba del archivo, igual
   que japon.js e history.js (que ya funcionan así).

La suite de arranque cazó el segundo problema al toque: verifica que cada
initX del arranque exista, y saltó cuando initSonido no estaba en su lista.

Modificados: js/agenda.js (import estático), js/app.js (initAgenda +
  initSonido en el arranque), sw.js, tests/suite-arranque.js

QUÉ VES HOY, SIN NADA HECHO:
  - En VOS → Agenda: el panel vacío con "+ Agendar algo". Ahora el botón
    ABRE la hoja: escribís qué es, elegís el día y lo agendás.
  - Si agendás algo para hoy, el aviso verde aparece arriba en HOY.

Suite: 148 tests. Todo en verde.
