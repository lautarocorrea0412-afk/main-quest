Entrega 7A — Tu rincón: la ceremonia de apertura
Caché SW: mq-entrega7a-ceremonia
La app debe mostrar en VOS: 'Entrega 7A · ceremonia · datos v7'

DATA_VERSION sube a 7: el perfil recuerda el día de la última ceremonia
(la ceremonia es 1 vez por día). Migración automática.

Nuevos:
  js/ceremonia.js — la secuencia de apertura. Booteo pixel de la espada
    del logo que SE TRANSFORMA en la luz de la lámpara; el cuarto se
    enciende de abajo hacia arriba; el avatar queda al frente. ~1.8s.

Decisiones respetadas (notas de Lautaro):
  - Una vez por día, no en cada apertura (la fricción diaria mata apps).
  - MUDA: en iOS el audio necesita un toque previo, y esto arranca solo.
  - Salteable con un tap en cualquier lado.
  - Respeta prefers-reduced-motion: si está activo, sin ceremonia.
  - El hanko NO se usa acá (es el premio por cumplir, no la puerta).

Modificados: js/store.js (v7), js/app.js, css/app.css, sw.js,
  tests/suite-store.js, tests/suite-ceremonia.js (nuevo)

Entrega 7B (después): poses de entrada contextuales, pestañeo irregular,
pelo con retraso. Se separó para no jugarse el rendimiento de una.

Suite: 98 tests. Todo en verde.
