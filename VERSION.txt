Entrega 14 — Sonido
Caché SW: mq-entrega14-sonido
La app debe mostrar en VOS: 'Entrega 14 · sonido · datos v10'

Sin cambios de datos (usa el ajuste sonido que ya existía, apagado).

Audio chiptune SINTETIZADO con Web Audio API. Cero archivos: cada efecto
son unas pocas notas de onda cuadrada/triangular generadas por código.

Efectos: cumplir principal (arpegio triunfal), secundaria (tic corto),
comprar (caja registradora suave), subir de nivel (fanfarria), logro
(brillo alto), entrar desde la ceremonia (acorde cálido).

TRAMPAS DE iOS CONTEMPLADAS:
  - El AudioContext arranca suspendido hasta el PRIMER gesto del usuario
    (toque). Se despierta ahí, nunca antes: si sonara solo, iOS lo callaría.
  - El interruptor físico de silencio del iPhone calla TODO esto sin que
    la app se entere. No es un bug; es iOS. La ayuda de Configuración lo
    advierte con todas las letras.
  - Arranca APAGADO. Nada suena hasta que lo prendas en Configuración
    (al prenderlo, suena un efecto de muestra).
  - El motor es a prueba de fallos: si el navegador se queja, queda mudo,
    nunca rompe la app (4 tests lo garantizan).

Modificados: js/sonido.js (nuevo), js/missions.js, js/economy.js, js/xp.js,
  js/achievements.js, js/ceremonia.js, js/config.js, js/app.js, sw.js,
  tests/suite-sonido.js

QUÉ VES (OÍS) HOY, SIN NADA HECHO:
  - Nada suena todavía: el sonido arranca apagado. Eso es correcto.
  - Si lo prendés en Configuración (VOS), a partir de ahí escuchás los
    efectos al cumplir misiones, comprar y subir de nivel. Antes de
    prenderlo, silencio total: no es un bug.
  - Recordá el interruptor de silencio del iPhone: si está en silencio,
    no vas a oír nada aunque el switch de la app esté encendido.

Suite: 140 tests. Todo en verde.
