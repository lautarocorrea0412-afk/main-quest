Entrega 8c — Distribución: hoja deslizable para Japón e Historia
Caché SW: mq-entrega8c-distribucion
La app debe mostrar en VOS: 'Entrega 8c · distribución · datos v9'

Sin cambios de datos ni de funcionalidad: solo distribución y experiencia.
Tomando la sección Avatar como referencia (lo importante visible, el
detalle a un toque), se resolvió que Japón e Historia crecían hacia abajo
y volvían la pestaña VOS interminable.

NUEVO COMPONENTE COMPARTIDO — js/ui.js abrirHoja():
  Una hoja que sube desde abajo (bottom sheet), como las apps nativas.
  El detalle se abre ENCIMA, ocupa solo lo necesario, con su propio
  scroll. La página principal queda compacta e intacta. Se cierra con la
  X, tocando el fondo, o arrastrando hacia abajo.

JAPÓN:
  - El panel principal queda compacto: barra protagonista (sin tocar) +
    últimos 3 movimientos del camino.
  - "Ver todo el camino" abre la hoja con el recorrido completo, en vez
    de expandir la página.

HISTORIA:
  - La pestaña VOS muestra un resumen: los últimos 5 días.
  - "Ver toda tu historia" abre la hoja con todo el recorrido, cargado
    por bloques de 10 (scroll interno, "Ver más días" dentro de la hoja).
  - El diario de cada día se sigue desplegando, en el resumen y en la hoja.

Modificados: js/ui.js (abrirHoja), js/japon.js, js/history.js,
  css/app.css, sw.js, js/app.js, tests/suite-distribucion.js (nuevo)

Suite: 114 tests. Todo en verde.
