Entrega 18c — Anti-flash + intro más ágil
Caché SW: mq-entrega18c-intro
La app debe mostrar en VOS: 'Entrega 18c · intro ágil · datos v13'

Sin cambios de datos. Dos ajustes:

1. EL "PANTALLAZO" AL ABRIR DESDE EL ÍCONO — arreglado. Al abrir una PWA
   en iOS hay un instante en blanco antes de que cargue el CSS. Ahora el
   fondo oscuro de la intro (#1a1622) se pinta ANTES de todo (estilo
   inline en el <head>), y el background_color del manifest y el
   theme-color coinciden con ese tono. Sin salto de color al arrancar.

2. INTRO MÁS ÁGIL. Comprimí la coreografía:
   - El avatar te nota a 1.1s (antes 1.6s).
   - Ofrece el mate a 1.9s (antes 2.8s).
   - El botón responde a 2.9s (antes 4.2s).
   - El revelado de la escena y la luz también entran más rápido.
   Sigue sintiéndose tranquila, pero llega antes al momento y a poder
   entrar.

Modificados: index.html (fondo anti-flash + theme-color), manifest.json
  (background_color), js/ceremonia.js (tiempos), css/app.css
  (transiciones), sw.js, js/app.js

Suite: 151 tests. Todo en verde.
