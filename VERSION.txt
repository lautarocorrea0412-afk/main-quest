Entrega 9 — Configuración
Caché SW: mq-entrega9-config
La app debe mostrar en VOS: 'Entrega 9 · configuración · datos v10'

DATA_VERSION sube a 10: bloque "ajustes" (hora del diario, sonido).
Migración automática: datos viejos se completan con los valores por
defecto (21:00, sonido apagado).

Nuevo panel Configuración en la pestaña VOS (js/config.js):
  - NOMBRE editable: se aplica al instante en el saludo y el perfil.
    Nunca queda vacío (si lo borrás, vuelve a "vos").
  - HORA DEL DIARIO configurable (18 a medianoche). El diario dejó de
    estar clavado a las 21: ahora journal.js lee data.ajustes.hora_diario.
    AVISO HONESTO en la ayuda: no es una alarma. Una PWA en iOS no puede
    despertar sola a una hora; esto controla cuándo APARECE el cierre del
    día al abrir la app.
  - INTERRUPTOR DE SONIDO: apagado por defecto, listo para la próxima
    entrega. La ayuda aclara que todavía no hay sonidos.

Modificados: js/store.js (v10 + ajustes), js/journal.js (hora del perfil),
  index.html, css/app.css (switch, select), js/app.js, sw.js,
  tests/helpers.js, tests/suite-store.js, tests/suite-arranque.js,
  tests/suite-config.js (nuevo)

Suite: 118 tests. Todo en verde.
