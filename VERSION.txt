Entrega 15 — Música de fondo + sonidos de navegación
Caché SW: mq-entrega15-musica
La app debe mostrar en VOS: 'Entrega 15 · música · datos v11'

DATA_VERSION sube a 11: flag de música (aparte del de efectos).

MÚSICA DE FONDO — pad ambiental SINTETIZADO (cero archivos):
  - No es una melodía: es una cama de acordes lentos (4 acordes que giran
    en loop, glissando suave entre ellos cada ~8s). Onda triangular +
    filtro pasa-bajos + volumen muy bajo, para que sea chill y no canse.
  - Interruptor APARTE de los efectos (decisión de Lautaro): podés tener
    música sin efectos o al revés. Los dos switches en Configuración.
  - Apagada por defecto. Fade-in/fade-out suave al prender/apagar.
  - Se reanuda al volver a la app (iOS suspende el audio en el fondo).

SONIDOS DE NAVEGACIÓN (efectos nuevos):
  - Cambiar de pestaña: un "pop" corto y bajo, casi táctil.
  - Probarse ropa / cambiar el avatar: un "blip" suave (solo si la prenda
    está desbloqueada).

Por qué pad sintetizado y no lo-fi real: mantener el "cero archivos / cero
costo" de toda la app. Música lo-fi de verdad necesitaría un .mp3; se puede
sumar más adelante si se decide gastar en eso. La misma advertencia de
siempre: el interruptor de silencio del iPhone calla todo esto.

Modificados: js/sonido.js (motor de música + efectos de navegación),
  js/store.js (v11), js/config.js (2º switch), js/app.js, js/ceremonia.js,
  js/avatar.js, sw.js, tests (helpers, store, config, sonido)

QUÉ VES (OÍS) HOY, SIN NADA HECHO:
  - Silencio: música y efectos arrancan apagados. Correcto.
  - Prendé cada uno por separado en VOS → Configuración. La música empieza
    con un fade-in suave; los sonidos de navegación se oyen al moverte por
    las pestañas y al cambiar de ropa.
  - Interruptor de silencio del iPhone: si está en silencio, no se oye nada.

Suite: 142 tests. Todo en verde.
